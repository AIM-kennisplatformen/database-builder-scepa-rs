# SCEPA frontend

The frontend application will live in this directory. Keeping it as a separate
workspace prevents its package manager, build output, and deployment setup from
being coupled to the Rust backend workspace.
